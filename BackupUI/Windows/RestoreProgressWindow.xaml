<Window
	x:Class="SecureServerBackup.Windows.RestoreProgressWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="Restore Progress"
	Width="550"
	Height="250"
	ResizeMode="CanMinimize"
	ShowInTaskbar="True"
	WindowStartupLocation="CenterOwner">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBlock
			x:Name="txtProgress"
			Grid.Row="0"
			Margin="0,0,0,15"
			FontSize="14"
			Text="Initializing restore..."
			TextWrapping="Wrap" />

		<ProgressBar
			x:Name="progressBar"
			Grid.Row="1"
			Height="25"
			Maximum="100"
			Minimum="0"
			Value="0" />

		<TextBlock
			x:Name="txtPercentage"
			Grid.Row="2"
			Margin="0,10,0,0"
			HorizontalAlignment="Center"
			FontSize="12"
			Text="0%" />

		<TextBlock
			x:Name="txtCurrentFile"
			Grid.Row="3"
			Margin="0,15,0,0"
			FontSize="11"
			Foreground="Gray"
			Text=""
			TextTrimming="CharacterEllipsis"
			TextWrapping="Wrap" />

		<StackPanel
			Grid.Row="4"
			Margin="0,20,0,0"
			HorizontalAlignment="Right"
			Orientation="Horizontal">
			<Button
				x:Name="btnHideClose"
				Width="120"
				Height="30"
				Click="Close_Click"
				Content="Hide Window" />
		</StackPanel>
	</Grid>
</Window>
