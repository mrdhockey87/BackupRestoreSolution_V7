<Window
	x:Class="SecureServerBackup.Windows.DiskSelectionWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="Select Target Disk"
	Width="600"
	Height="500"
	ResizeMode="NoResize"
	ShowInTaskbar="False"
	WindowStartupLocation="CenterOwner"
	WindowStyle="SingleBorderWindow">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<!--  Header  -->
		<TextBlock
			Grid.Row="0"
			Margin="0,0,0,15"
			FontSize="16"
			FontWeight="Bold"
			Text="Select Target Disk for Clone Operation" />

		<!--  Instructions  -->
		<Border
			Grid.Row="1"
			Margin="0,0,0,15"
			Padding="10"
			Background="{StaticResource InfoBackground}"
			BorderBrush="#2196F3"
			BorderThickness="1"
			CornerRadius="4">
			<StackPanel>
				<TextBlock
					Margin="0,0,0,5"
					FontWeight="SemiBold"
					Text="Select the destination disk for the clone operation:" />
				<TextBlock
					FontSize="11"
					Foreground="{StaticResource SecondaryText}"
					Text="- Only available physical disks are shown" />
				<TextBlock
					FontSize="11"
					Foreground="{StaticResource SecondaryText}"
					Text="- Source disk is excluded from the list" />
				<TextBlock
					Margin="0,5,0,0"
					FontSize="11"
					FontWeight="Bold"
					Foreground="#D32F2F"
					Text="- All data on the target disk will be REPLACED" />
			</StackPanel>
		</Border>

		<!--  Disk List  -->
		<Border
			Grid.Row="2"
			BorderBrush="#BDBDBD"
			BorderThickness="1"
			CornerRadius="4">
			<ScrollViewer VerticalScrollBarVisibility="Auto">
				<ListBox
					Name="lstDisks"
					Padding="5"
					BorderThickness="0"
					SelectionChanged="DiskList_SelectionChanged"
					SelectionMode="Single">
					<ListBox.ItemContainerStyle>
						<Style TargetType="ListBoxItem">
							<Setter Property="Padding" Value="10" />
							<Setter Property="Margin" Value="2" />
							<Setter Property="BorderThickness" Value="1" />
							<Setter Property="BorderBrush" Value="#E0E0E0" />
							<Setter Property="Background" Value="White" />
							<Style.Triggers>
								<Trigger Property="IsMouseOver" Value="True">
									<Setter Property="Background" Value="#F5F5F5" />
								</Trigger>
								<Trigger Property="IsSelected" Value="True">
									<Setter Property="Background" Value="#E3F2FD" />
									<Setter Property="BorderBrush" Value="#2196F3" />
								</Trigger>
							</Style.Triggers>
						</Style>
					</ListBox.ItemContainerStyle>
					<ListBox.ItemTemplate>
						<DataTemplate>
							<StackPanel>
								<TextBlock
									FontSize="14"
									FontWeight="Bold"
									Text="{Binding DisplayName}" />
								<TextBlock
									Margin="0,3,0,0"
									FontSize="12"
									Foreground="{StaticResource SecondaryText}"
									Text="{Binding Details}" />
							</StackPanel>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</ScrollViewer>
		</Border>

		<!--  Buttons  -->
		<StackPanel
			Grid.Row="3"
			Margin="0,15,0,0"
			HorizontalAlignment="Right"
			Orientation="Horizontal">
			<Button
				Name="btnSelect"
				Width="100"
				Height="35"
				Margin="0,0,10,0"
				Click="Select_Click"
				Content="Select Disk"
				IsEnabled="False" />
			<Button
				Name="btnCancel"
				Width="100"
				Height="35"
				Click="Cancel_Click"
				Content="Cancel" />
		</StackPanel>
	</Grid>
</Window>
