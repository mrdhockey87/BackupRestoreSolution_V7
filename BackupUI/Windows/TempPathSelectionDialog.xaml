<Window
	x:Class="SecureServerBackup.Windows.TempPathSelectionDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="Select Temporary Path for Mount"
	Width="550"
	Height="340"
	Background="{StaticResource WindowBackground}"
	BorderBrush="{StaticResource LightBorderBrush}"
	BorderThickness="1"
	ResizeMode="NoResize"
	WindowStartupLocation="CenterOwner">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBlock
			Grid.Row="0"
			Margin="0,0,0,15"
			FontSize="16"
			FontWeight="Bold"
			Text="Temporary Path for Mount Operation" />

		<TextBlock
			Grid.Row="1"
			Margin="0,0,0,10"
			Foreground="{StaticResource SecondaryText}"
			TextWrapping="Wrap">
			<Run Text="The Secure Server Backup requires a temporary directory to decompress and process backup image data during mount operations." />
			<LineBreak /><LineBreak />
			<Run Text="Choose a location with adequate free space (several GB may be needed for large backups)." />
		</TextBlock>

		<Label
			Grid.Row="2"
			Margin="0,5,0,5"
			Content="Temporary Path:" />

		<Grid Grid.Row="3" Margin="0,0,0,15">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<TextBox
				Name="txtTempPath"
				Grid.Column="0"
				Padding="5"
				VerticalContentAlignment="Center"
				Background="{StaticResource PanelBackground}"
				IsReadOnly="True" />
			<Button
				Grid.Column="1"
				Width="80"
				Height="28"
				Margin="10,0,0,0"
				Click="Browse_Click"
				Content="Browse..." />
		</Grid>

		<StackPanel
			Grid.Row="4"
			Margin="0,0,0,15"
			Orientation="Vertical">
			<TextBlock
				Name="txtSpaceInfo"
				FontSize="12"
				Foreground="{StaticResource SecondaryText}"
				Text="Checking available space..." />
		</StackPanel>

		<StackPanel
			Grid.Row="5"
			HorizontalAlignment="Right"
			Orientation="Horizontal">
			<Button
				Width="80"
				Height="28"
				Margin="0,0,10,0"
				Click="OK_Click"
				Content="OK"
				IsDefault="True" />
			<Button
				Width="80"
				Height="28"
				Click="Cancel_Click"
				Content="Cancel"
				IsCancel="True" />
		</StackPanel>
	</Grid>
</Window>