<Window
	x:Class="SecureServerBackup.Windows.ActivityManagementWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="Activity Management - Backup Job Logs"
	Width="1000"
	Height="600"
	WindowStartupLocation="CenterScreen">
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<!--  Header  -->
		<StackPanel
			Grid.Row="0"
			Height="50"
			Margin="0,0,0,10"
			Background="{StaticResource PanelBackground}"
			Orientation="Horizontal">
			<TextBlock
				Margin="15,0,20,0"
				VerticalAlignment="Center"
				FontSize="18"
				FontWeight="Bold"
				Text="Backup Job Activity Logs" />
			<Button
				Width="100"
				Height="32"
				Margin="5"
				Click="Refresh_Click"
				Content="Refresh" />
			<Button
				Width="140"
				Height="32"
				Margin="5"
				Click="ViewAllActivities_Click"
				Content="View All Activities" />
		</StackPanel>

		<!--  Job Logs List  -->
		<DataGrid
			Name="dgJobLogs"
			Grid.Row="1"
			Margin="0,0,0,10"
			AlternatingRowBackground="#F9F9F9"
			AutoGenerateColumns="False"
			CanUserResizeColumns="True"
			GridLinesVisibility="Horizontal"
			IsReadOnly="True"
			MouseDoubleClick="JobLog_DoubleClick"
			SelectionMode="Single">
			<DataGrid.Columns>
				<DataGridTextColumn
					Width="170"
					Binding="{Binding JobName}"
					Header="Job Name" />
				<DataGridTextColumn
					Width="120"
					Binding="{Binding TotalActivities}"
					Header="Total Activities"
					HeaderStringFormat="Total Activities" />
				<DataGridTextColumn
					Width="150"
					Binding="{Binding LastActivity, StringFormat='{}{0:MM/dd/yyyy HH:mm:ss}'}"
					Header="Last Activity" />
				<DataGridTextColumn
					Width="110"
					Binding="{Binding SuccessCount}"
					Header="Success Count">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="Foreground" Value="Green" />
							<Setter Property="FontWeight" Value="SemiBold" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTextColumn
					Width="110"
					Binding="{Binding WarningCount}"
					Header="Warning Count">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="Foreground" Value="Orange" />
							<Setter Property="FontWeight" Value="SemiBold" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTextColumn
					Width="100"
					Binding="{Binding ErrorCount}"
					Header="Error Count">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="Foreground" Value="Red" />
							<Setter Property="FontWeight" Value="SemiBold" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTemplateColumn Width="200" Header="Actions">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button
									Width="100"
									Height="25"
									Margin="5,0"
									Click="ViewJobDetails_Click"
									Content="View Details"
									Tag="{Binding JobName}" />
								<Button
									Width="80"
									Height="25"
									Margin="5,0"
									Click="ExportJobLog_Click"
									Content="Export"
									Tag="{Binding JobName}" />
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<!--  Footer Info  -->
		<Border
			Grid.Row="2"
			Padding="10"
			Background="{StaticResource PanelBackground}"
			BorderBrush="{StaticResource LightBorderBrush}"
			BorderThickness="1">
			<TextBlock
				Name="txtStatus"
				FontSize="12"
				Foreground="{StaticResource SecondaryText}"
				Text="Double-click a job to view detailed activities" />
		</Border>
	</Grid>
</Window>
